spring:
  application:
    name: product-catalog-service-test
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=PostgreSQL;NON_KEYWORDS=VALUE
    username: sa
    password: 
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect
  flyway:
    enabled: false
  cloud:
    config:
      enabled: false
  eureka:
    client:
      enabled: false
  cache:
    type: none
  data:
    redis:
      repositories:
        enabled: false

eureka:
  client:
    enabled: false

logging:
  level:
    org.de013.productcatalogservice: DEBUG
    org.springframework.security: WARN
    org.hibernate.SQL: DEBUG
